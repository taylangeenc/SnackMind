using SnackMindData.Repository.Base;

using SnackMindModel.Model.Payment.Payment;

namespace SnackMindData.Repository.IRepository.Payment.Payment
{
    public interface IPaymentTypeRepository : ISmRepository<mdlPaymentType>
    {
    }
}